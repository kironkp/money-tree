"""Grounding: the hard numbers a dossier is built from.

Deliberately NOT named after, and deliberately separate from, the walk-forward
machinery that already owns the word "research" in this app (`views/research.py`,
`manage.py auto_research`, the nightly plist). That is parameter search; this is
reading the filings.
"""
